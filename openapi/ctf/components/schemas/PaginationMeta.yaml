type: object
properties:
  current_page:
    type: integer
  current_page_url:
    type: string
    format: uri
  from:
    type: integer
    nullable: true
  path:
    type: string
    format: uri
  per_page:
    type: integer
  to:
    type: integer
    nullable: true
