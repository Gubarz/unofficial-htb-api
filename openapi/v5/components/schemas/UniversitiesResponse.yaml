description: Schema definition for Universities list responses
type: object
properties:
  data:
    $ref: ./UniversitiesData.yaml
  links:
    $ref: ./Links.yaml
  meta:
    $ref: ./Meta.yaml
required:
  - data
  - links
  - meta
