description: Field to sort by
in: query
name: sort_by
required: false
schema:
  enum:
    - release_date
    - name
    - user_owns
    - system_owns
    - rating
    - user_difficulty
  type: string
