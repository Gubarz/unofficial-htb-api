type: object
properties:
  status:
    type: string
    nullable: true
  expires_at:
    type: string
    format: date-time
    nullable: true
  ip:
    type: string
    nullable: true
  ports:
    $ref: ./IntArray.yaml
description: Alternate structure of PlayInfo used in Sherlocks and Challenges
