content:
  application/json:
    schema:
      $ref: ../schemas/MachineRecommendedRetiredResponse.yaml
description: Recommended Retired Machines Response
headers:
  x-ratelimit-limit:
    $ref: ../headers/XRateLimitHeader.yaml
  x-ratelimit-remaining:
    $ref: ../headers/XRateLimitRemainingHeader.yaml
